import React from 'react';
import { SafeAreaView, StatusBar, Text } from "react-native";
import Navigation from "./app/natigation/Navigation.tsx";

const EntryPoint = () => {

  return (
      <Navigation />
  );
};

export default EntryPoint;